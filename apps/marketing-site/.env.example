# apps/marketing-site currently needs no environment variables - it is a
# fully static content site (no API calls, no secrets). Kept as its own
# file, separate from apps/web and apps/api, per this project's
# per-app-env convention. Add analytics keys (Plausible/PostHog/etc.)
# here if/when analytics is added to the landing page - never share
# analytics config with apps/web.
